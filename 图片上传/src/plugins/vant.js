import Vue from 'vue';
import { Button, Popup, List, Notify, Dialog, Icon, Toast, PullRefresh  } from 'vant';

Vue
  .use(Button)
  .use(Popup )
  .use(List)
  .use(Notify)
  .use(Dialog)
  .use(Icon)
  .use(Toast)
  .use(PullRefresh )